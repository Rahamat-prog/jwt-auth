# this project future : 
-> at the first render of the app a login form will be display where user can provide their correct user id and password and login after login we just simply display the user details like user name , email id with using tailwind.
-> user can login from the client side 
-> and retrive the user data from the backend and sent to client 
-> right now if we want to logout the user so we need to go browser inspect and go to tab application -> here inside cookie we will get the token if we delete it so its logout and then so again the login from.
# pending work : 
letter we build resigtration form so user can registration from client till now we have build the registration process which is actully work through postman. but right now not able to do from the client side.
# logout - > 
later will build logout methot so user can logout form the client side , right now we have just logout it throught postman via token so besically backend is ready we need to build client side logout method and connect from the backend and boom! 


# tech stack use -> 
React.js
Tailwind
JavaScript
Node.js
Express.js
MongoDB
Tools use -> Postman 

